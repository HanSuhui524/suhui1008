'use strict';
const LN='----------------'
const num=require('../index.js')
function ln(){console.log(LN);}
console.dir(num);
ln();
const one=num.grade.make(1);
console.log('one')
console.dir(one);
ln();
const firstGrade='1st Grade Math'
console.log('test one',firstGrade,'string')
console.dir(one.test(firstGrade).grade());
ln();

const secGrade='2nd Grade Math'
console.log('test one',secGrade,'string')
console.dir(one.test(secGrade).grade());
ln();

const levels=num.grade.levels;
console.log('math secGrade',secGrade)
console.log(levels.match(secGrade));