let mongoose;_d9c‍.x([["default",()=>_d9c‍.o]]);_d9c‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

const ProductSchema = new Schema({
    title: String,
    body: String,
    tags: [String], //문자열로 이루어진 배열
    price: Number,
    image: [String],
    quantity: Number,
    publishedDate: {
    type: Date,
    default: Date.now(), // 현재 날짜를 기본값으로 지정
    },
});

const Product = mongoose.model('Product', ProductSchema); //모델 생성
_d9c‍.d(Product);